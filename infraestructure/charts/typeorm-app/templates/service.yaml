apiVersion: v1
kind: Service
metadata:
  name: {{ include "typeorm-app.fullname" . }}
  labels:
    {{- include "typeorm-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "typeorm-app.selectorLabels" . | nindent 4 }}
